import { useEffect, useMemo, useState, type ReactNode } from 'react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { ArrowDownRight, ArrowLeft, ArrowRight, BarChart3, Bell, BookOpen, Check, CheckCircle2, ChevronDown, Clock3, FileText, Headphones, LayoutDashboard, LifeBuoy, LockKeyhole, Menu, MessageSquare, MoreHorizontal, Play, Plus, Search, Settings, Sparkles, Target, Users, X } from 'lucide-react';
import { Link, Route, Switch, useLocation, useParams, Router as WouterRouter } from 'wouter';
import { ErrorBoundary } from '@/components/error-boundary';
import { Toaster } from '@/components/ui/toaster';
import { TooltipProvider } from '@/components/ui/tooltip';
import NotFound from '@/pages/not-found';
import { BrandMark } from '@/components/brand-mark';
import { assignments, navItems, sessions, student, type Assignment, type Session } from '@/lib/mock-data';
import { LearnerProvider, useLearnerStore } from '@/lib/learner-store';

const queryClient = new QueryClient();

function PageMeta({ title, description }: { title: string; description?: string }) {
  useEffect(() => {
    document.title = `${title} — Social Seller AI`;
    if (description) {
      let tag = document.querySelector('meta[name="description"]');
      if (!tag) { tag = document.createElement('meta'); tag.setAttribute('name', 'description'); document.head.appendChild(tag); }
      tag.setAttribute('content', description);
    }
  }, [title, description]);
  return null;
}

function Button({ children, variant = 'primary', className = '', ...props }: { children: ReactNode; variant?: 'primary' | 'ghost' | 'outline' | 'accent'; className?: string } & React.ButtonHTMLAttributes<HTMLButtonElement>) {
  const styles = {
    primary: 'bg-primary text-primary-foreground hover:bg-primary/90',
    ghost: 'bg-transparent text-foreground hover:bg-muted',
    outline: 'border border-border bg-card text-foreground hover:border-foreground/40',
    accent: 'bg-accent text-primary hover:brightness-95',
  };
  return <button className={`inline-flex items-center justify-center gap-2 rounded-sm px-4 py-2.5 text-sm font-semibold transition-all duration-200 active:translate-y-px disabled:cursor-not-allowed disabled:opacity-50 ${styles[variant]} ${className}`} {...props}>{children}</button>;
}

function PublicNav() {
  const [open, setOpen] = useState(false);
  return <header className="absolute left-0 right-0 top-0 z-20">
    <div className="mx-auto flex max-w-[1240px] items-center justify-between px-5 py-5 lg:px-8">
      <Link href="/" data-testid="link-brand-home"><BrandMark /></Link>
      <nav className="hidden items-center gap-7 md:flex" aria-label="Main navigation">
        <a href="#programme" className="text-sm text-primary/70 transition-colors hover:text-primary" data-testid="link-programme">The programme</a>
        <a href="#method" className="text-sm text-primary/70 transition-colors hover:text-primary" data-testid="link-method">The method</a>
        <a href="#mentor" className="text-sm text-primary/70 transition-colors hover:text-primary" data-testid="link-mentor">Your mentor</a>
        <Link href="/login" className="text-sm text-primary/70 transition-colors hover:text-primary" data-testid="link-login">Sign in</Link>
        <Link href="/signup" className="inline-flex items-center gap-2 bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground transition-transform hover:-translate-y-0.5" data-testid="link-start-learning">Start learning <ArrowRight size={15} /></Link>
      </nav>
      <button className="grid size-10 place-items-center border border-primary/15 md:hidden" onClick={() => setOpen(!open)} aria-label="Toggle menu" data-testid="button-toggle-menu">{open ? <X size={19} /> : <Menu size={19} />}</button>
    </div>
    {open && <div className="mx-5 border border-border bg-card p-4 shadow-lg md:hidden">
      <div className="grid gap-1">
        <a href="#programme" onClick={() => setOpen(false)} className="p-3 text-sm" data-testid="link-mobile-programme">The programme</a>
        <a href="#method" onClick={() => setOpen(false)} className="p-3 text-sm" data-testid="link-mobile-method">The method</a>
        <Link href="/login" onClick={() => setOpen(false)} className="p-3 text-sm" data-testid="link-mobile-login">Sign in</Link>
        <Link href="/signup" onClick={() => setOpen(false)} className="bg-primary p-3 text-sm font-semibold text-primary-foreground" data-testid="link-mobile-start">Start learning</Link>
      </div>
    </div>}
  </header>;
}

function SignalGraphic() {
  return <div className="relative min-h-[340px] overflow-hidden border border-primary/15 bg-primary p-5 text-primary-foreground sm:min-h-[430px] sm:p-8">
    <div className="absolute inset-0 opacity-30" style={{ backgroundImage: 'linear-gradient(rgba(243,240,231,.16) 1px, transparent 1px), linear-gradient(90deg, rgba(243,240,231,.16) 1px, transparent 1px)', backgroundSize: '28px 28px' }} />
    <div className="absolute -right-16 -top-12 size-52 rounded-full border border-secondary/50 sm:size-72" />
    <div className="absolute -bottom-24 -left-20 size-64 rounded-full border border-accent/40 sm:size-80" />
    <div className="relative flex h-full min-h-[300px] flex-col justify-between sm:min-h-[370px]">
      <div className="flex items-center justify-between font-mono-ui text-[10px] uppercase tracking-[.18em] text-primary-foreground/60"><span>Signal / 001</span><span>30—day protocol</span></div>
      <div className="relative mx-auto my-10 w-full max-w-[390px]">
        <div className="h-px w-full bg-primary-foreground/20" />
        <div className="absolute -top-1 left-[18%] size-2 rounded-full bg-secondary shadow-[0_0_0_6px_rgba(216,240,75,.12)]" />
        <div className="absolute -top-1 left-[61%] size-2 rounded-full bg-accent shadow-[0_0_0_6px_rgba(240,109,82,.12)]" />
        <svg viewBox="0 0 400 145" className="absolute -top-[72px] left-0 w-full overflow-visible" aria-label="Momentum line">
          <path d="M0 132 C40 131, 36 89, 76 97 S110 110, 140 81 S181 46, 205 73 S233 102, 258 61 S292 36, 320 48 S348 67, 400 7" fill="none" stroke="#d8f04b" strokeWidth="3" />
          <circle cx="400" cy="7" r="5" fill="#f06d52" />
        </svg>
        <div className="mt-8 grid grid-cols-3 gap-2 font-mono-ui text-[9px] uppercase tracking-[.13em] text-primary-foreground/50"><span>notice</span><span className="text-center">shape</span><span className="text-right text-secondary">ship</span></div>
      </div>
      <div className="flex items-end justify-between"><p className="max-w-[230px] font-display text-3xl leading-[.9] sm:text-4xl">Make your<br /><em className="text-secondary">useful</em> visible.</p><span className="float-slow grid size-12 place-items-center rounded-full border border-primary-foreground/30 text-secondary"><ArrowUpRightIcon /></span></div>
    </div>
  </div>;
}

function ArrowUpRightIcon() { return <ArrowRight size={18} className="-rotate-45" />; }

function Home() {
  return <div className="grain min-h-[100dvh] bg-background">
    <PageMeta title="Launch your AI business in 30 days" description="A focused, high-touch AI business programme by Lakshit Sethiya." />
    <PublicNav />
    <main>
      <section className="shell-grid relative overflow-hidden border-b border-border">
        <div className="mx-auto grid max-w-[1240px] gap-10 px-5 pb-16 pt-32 sm:pb-24 lg:grid-cols-[1.04fr_.96fr] lg:items-end lg:gap-16 lg:px-8 lg:pb-28 lg:pt-44">
          <div className="reveal">
            <div className="mb-7 flex items-center gap-3 font-mono-ui text-[10px] font-medium uppercase tracking-[.19em] text-muted-foreground"><span className="h-px w-7 bg-accent" /> 30-day AI mastermind programme</div>
            <h1 className="max-w-[730px] text-balance text-6xl leading-[.88] tracking-[-.055em] sm:text-8xl lg:text-[7.9rem]">Turn AI<br /><span className="signal-line font-display italic">into signal.</span></h1>
            <p className="mt-9 max-w-[465px] text-base leading-7 text-muted-foreground sm:text-lg">A focused studio for ambitious creators and founders who want to build an online business around what they know — and what AI makes possible.</p>
            <div className="mt-9 flex flex-wrap items-center gap-3"><Link href="/signup" className="inline-flex items-center gap-3 bg-primary px-5 py-3.5 text-sm font-semibold text-primary-foreground transition-transform hover:-translate-y-1" data-testid="link-hero-enrol">Enter the programme <ArrowRight size={16} /></Link><a href="#programme" className="inline-flex items-center gap-2 px-3 py-3.5 text-sm font-semibold text-primary/70 hover:text-primary" data-testid="link-hero-explore">See how it works <ArrowDownRight size={16} /></a></div>
            <div className="mt-12 flex items-center gap-4 border-t border-border pt-5"><div className="flex -space-x-2">{['AS', 'NP', 'RK'].map((initials) => <span key={initials} className="grid size-8 place-items-center rounded-full border-2 border-background bg-muted font-mono-ui text-[9px] font-medium">{initials}</span>)}</div><p className="text-xs leading-5 text-muted-foreground"><strong className="font-semibold text-foreground">10 Lakh+ subscribers</strong><br />learning in public with Lakshit</p></div>
          </div>
          <div className="reveal reveal-delay-2"><SignalGraphic /></div>
        </div>
        <div className="mx-auto flex max-w-[1240px] flex-wrap items-center gap-x-10 gap-y-3 border-t border-border px-5 py-5 font-mono-ui text-[10px] uppercase tracking-[.15em] text-muted-foreground lg:px-8"><span>Practical AI craft</span><span className="text-accent">/</span><span>Live learning</span><span className="text-accent">/</span><span>Founder context</span><span className="text-accent">/</span><span>One useful business</span></div>
      </section>
      <section id="programme" className="mx-auto max-w-[1240px] px-5 py-20 sm:py-28 lg:px-8">
        <div className="grid gap-12 lg:grid-cols-[.65fr_1.35fr] lg:gap-24">
          <div><p className="font-mono-ui text-[10px] uppercase tracking-[.2em] text-accent">01 / The programme</p><h2 className="mt-5 max-w-sm text-5xl leading-[.94] tracking-[-.045em] sm:text-6xl">Thirty days.<br /><span className="font-display italic">One real offer.</span></h2><p className="mt-6 max-w-sm leading-7 text-muted-foreground">Not a library of videos to collect. A paced sequence of decisions, practice and feedback that turns scattered curiosity into a clear first move.</p></div>
          <div className="divide-y divide-border border-y border-border">{[['01', 'Notice', 'Find the useful edge in your experience.'], ['02', 'Shape', 'Turn that edge into a promise people understand.'], ['03', 'Ship', 'Put the offer in the world and learn from the response.']].map(([number, title, copy], index) => <div key={number} className="group grid gap-4 py-7 sm:grid-cols-[70px_1fr_auto] sm:items-center"><span className="font-mono-ui text-xs text-accent">{number}</span><div><h3 className="text-2xl font-semibold tracking-tight">{title}</h3><p className="mt-1 text-sm leading-6 text-muted-foreground">{copy}</p></div><span className="hidden size-9 place-items-center rounded-full border border-border transition-all group-hover:border-accent group-hover:bg-accent sm:grid"><ArrowUpRightIcon /></span></div>)}</div>
        </div>
      </section>
      <section id="method" className="bg-primary text-primary-foreground">
        <div className="mx-auto grid max-w-[1240px] gap-12 px-5 py-20 sm:py-28 lg:grid-cols-[.82fr_1.18fr] lg:gap-24 lg:px-8">
          <div><p className="font-mono-ui text-[10px] uppercase tracking-[.2em] text-secondary">02 / The studio model</p><h2 className="mt-5 max-w-md text-5xl leading-[.93] tracking-[-.045em] sm:text-6xl">High touch,<br /><span className="font-display italic text-secondary">low noise.</span></h2><p className="mt-7 max-w-md leading-7 text-primary-foreground/65">Every week has a job. The room stays small enough for thoughtful feedback and practical enough to keep moving.</p><Link href="/signup" className="mt-9 inline-flex items-center gap-2 border-b border-secondary pb-2 text-sm font-semibold text-secondary" data-testid="link-method-join">See your first week <ArrowRight size={15} /></Link></div>
          <div className="grid gap-3 sm:grid-cols-2">{[{ icon: <Target size={19} />, title: 'A sharper lens', copy: 'Learn to spot valuable problems before you reach for a tool.' }, { icon: <MessageSquare size={19} />, title: 'Live calibration', copy: 'Bring your thinking into a room where useful questions get asked.' }, { icon: <BookOpen size={19} />, title: 'Practical lessons', copy: 'Short masterclasses designed to be used the same day.' }, { icon: <Users size={19} />, title: 'A serious room', copy: 'Find peers who are building, testing and shipping alongside you.' }].map((item) => <div key={item.title} className="border border-primary-foreground/15 p-6 transition-colors hover:border-secondary/70"><div className="text-secondary">{item.icon}</div><h3 className="mt-12 text-lg font-semibold">{item.title}</h3><p className="mt-2 text-sm leading-6 text-primary-foreground/55">{item.copy}</p></div>)}</div>
        </div>
      </section>
      <section id="mentor" className="mx-auto max-w-[1240px] px-5 py-20 sm:py-28 lg:px-8">
        <div className="grid gap-12 lg:grid-cols-[1fr_1fr] lg:items-center lg:gap-24">
          <div className="relative min-h-[360px] overflow-hidden bg-muted p-7 sm:min-h-[430px] sm:p-10"><div className="absolute right-8 top-8 size-24 rounded-full border border-accent sm:size-32" /><div className="absolute bottom-0 left-0 h-1/2 w-3/5 bg-primary" /><div className="absolute bottom-8 left-8 font-mono-ui text-[10px] uppercase tracking-[.18em] text-primary-foreground/70 sm:bottom-10 sm:left-10">Lakshit Sethiya<br /><span className="text-secondary">Teacher / builder</span></div><div className="absolute bottom-24 right-9 h-36 w-24 -rotate-12 border border-primary/40 sm:right-20 sm:h-48 sm:w-32" /><div className="absolute bottom-32 right-16 h-24 w-16 rotate-12 bg-accent/80 sm:right-28 sm:h-32 sm:w-24" /></div>
          <div><p className="font-mono-ui text-[10px] uppercase tracking-[.2em] text-accent">03 / Your mentor</p><h2 className="mt-5 text-5xl leading-[.95] tracking-[-.045em] sm:text-6xl">Learn the<br /><span className="font-display italic">way builders think.</span></h2><p className="mt-7 max-w-md leading-7 text-muted-foreground">Lakshit Sethiya has helped make AI feel useful to a massive learning community. This programme distils that practical lens into a close, 30-day working room.</p><div className="mt-9 grid max-w-md grid-cols-2 gap-4 border-t border-border pt-5"><div><p className="text-2xl font-semibold tracking-tight">10 Lakh+</p><p className="mt-1 text-xs text-muted-foreground">subscribers</p></div><div><p className="text-2xl font-semibold tracking-tight">437,787+</p><p className="mt-1 text-xs text-muted-foreground">students reached</p></div></div></div>
        </div>
      </section>
      <section className="border-y border-border bg-accent/15"><div className="mx-auto grid max-w-[1240px] gap-7 px-5 py-14 sm:grid-cols-[1fr_auto] sm:items-center sm:px-8"><div><p className="font-mono-ui text-[10px] uppercase tracking-[.2em] text-accent">Ready when you are</p><h2 className="mt-3 max-w-xl text-4xl leading-none tracking-[-.04em] sm:text-5xl">Your next useful idea is closer than it feels.</h2></div><Link href="/signup" className="inline-flex w-fit items-center gap-3 bg-primary px-5 py-3.5 text-sm font-semibold text-primary-foreground transition-transform hover:-translate-y-1" data-testid="link-final-enrol">Join the 30-day room <ArrowRight size={16} /></Link></div></section>
    </main>
    <footer className="mx-auto flex max-w-[1240px] flex-col gap-4 px-5 py-8 text-xs text-muted-foreground sm:flex-row sm:items-center sm:justify-between lg:px-8"><BrandMark compact /><p>© 2024 Social Seller AI. Built for useful momentum.</p></footer>
  </div>;
}

function AuthPage({ mode }: { mode: 'login' | 'signup' }) {
  const [, setLocation] = useLocation();
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const isSignup = mode === 'signup';
  const submit = (event: React.FormEvent) => { event.preventDefault(); setLocation('/app'); };
  return <div className="grain grid min-h-[100dvh] bg-background lg:grid-cols-[.82fr_1.18fr]">
    <PageMeta title={isSignup ? 'Join the programme' : 'Sign in'} />
    <div className="hidden bg-primary p-10 text-primary-foreground lg:flex lg:flex-col lg:justify-between"><Link href="/" data-testid="link-auth-brand"><BrandMark /></Link><div><p className="font-mono-ui text-[10px] uppercase tracking-[.2em] text-secondary">The 30-day AI mastermind</p><h1 className="mt-5 max-w-lg text-7xl leading-[.88] tracking-[-.055em]">Your useful<br /><span className="font-display italic text-secondary">work starts here.</span></h1><p className="mt-7 max-w-sm text-sm leading-6 text-primary-foreground/60">A focused learning room for creators and founders building their next chapter with AI.</p></div><p className="font-mono-ui text-[10px] uppercase tracking-[.15em] text-primary-foreground/40">Signal / Shape / Ship</p></div>
    <div className="flex items-center justify-center px-5 py-12 sm:px-10"><div className="w-full max-w-[430px]"><Link href="/" className="mb-16 inline-block lg:hidden" data-testid="link-mobile-auth-brand"><BrandMark /></Link><p className="font-mono-ui text-[10px] uppercase tracking-[.2em] text-accent">{isSignup ? 'Start your studio pass' : 'Welcome back'}</p><h1 className="mt-4 text-4xl tracking-[-.04em]">{isSignup ? 'Make something useful.' : 'Pick up your signal.'}</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">{isSignup ? 'Create your learner profile and enter the first session.' : 'Your sessions, notes and next move are waiting.'}</p><form onSubmit={submit} className="mt-9 space-y-5"><label className="block"><span className="mb-2 block text-xs font-semibold">Email address</span><input required value={email} onChange={(e) => setEmail(e.target.value)} type="email" placeholder="you@studio.com" className="w-full border border-border bg-card px-4 py-3 text-sm outline-none transition-colors placeholder:text-muted-foreground/70 focus:border-primary" data-testid="input-auth-email" /></label><label className="block"><span className="mb-2 block text-xs font-semibold">Password</span><input required minLength={6} value={password} onChange={(e) => setPassword(e.target.value)} type="password" placeholder="Six characters or more" className="w-full border border-border bg-card px-4 py-3 text-sm outline-none transition-colors placeholder:text-muted-foreground/70 focus:border-primary" data-testid="input-auth-password" /></label><Button type="submit" className="w-full py-3.5" data-testid="button-auth-submit">{isSignup ? 'Create learner profile' : 'Sign in to your studio'} <ArrowRight size={16} /></Button></form><div className="mt-7 flex items-center gap-3 text-xs text-muted-foreground"><LockKeyhole size={14} /> Your learning data stays private.</div><p className="mt-10 text-sm text-muted-foreground">{isSignup ? 'Already have an account?' : 'New to the studio?'} <Link href={isSignup ? '/login' : '/signup'} className="font-semibold text-foreground underline underline-offset-4" data-testid="link-auth-switch">{isSignup ? 'Sign in' : 'Create your profile'}</Link></p></div></div>
  </div>;
}

function AppShell({ children }: { children: ReactNode }) {
  const [location, setLocation] = useLocation();
  const [mobileNav, setMobileNav] = useState(false);
  return <div className="min-h-[100dvh] bg-background">
    <aside className={`fixed inset-y-0 left-0 z-30 w-[244px] border-r border-sidebar-border bg-sidebar px-4 py-5 text-sidebar-foreground transition-transform duration-300 lg:translate-x-0 ${mobileNav ? 'translate-x-0' : '-translate-x-full'}`}>
      <div className="flex items-center justify-between px-2"><Link href="/app" data-testid="link-app-brand"><BrandMark /></Link><button className="text-sidebar-foreground/60 lg:hidden" onClick={() => setMobileNav(false)} aria-label="Close navigation" data-testid="button-close-nav"><X size={18} /></button></div>
      <div className="mt-12 px-2 font-mono-ui text-[9px] uppercase tracking-[.2em] text-sidebar-foreground/40">Your studio</div>
      <nav className="mt-3 grid gap-1">{navItems.map((item, index) => { const active = index === 0 ? location === '/app' : location.includes(item.label.toLowerCase()); return <Link key={item.href} href={item.href} onClick={() => setMobileNav(false)} className={`flex items-center gap-3 px-3 py-2.5 text-sm transition-colors ${active ? 'bg-sidebar-accent text-sidebar-accent-foreground' : 'text-sidebar-foreground/60 hover:bg-sidebar-accent/70 hover:text-sidebar-foreground'}`} data-testid={`link-sidebar-${item.label.toLowerCase()}`}>{index === 0 ? <LayoutDashboard size={16} /> : index === 1 ? <Play size={16} /> : <FileText size={16} />}{item.label}</Link>; })}</nav>
      <div className="mt-10 px-2 font-mono-ui text-[9px] uppercase tracking-[.2em] text-sidebar-foreground/40">Keep moving</div>
      <div className="mt-3 border border-sidebar-border p-4"><div className="flex items-center justify-between text-xs"><span className="text-sidebar-foreground/60">Your momentum</span><span className="text-secondary">4 days</span></div><div className="mt-3 h-1 bg-sidebar-accent"><div className="h-full w-[28%] bg-secondary" /></div><p className="mt-3 text-xs leading-5 text-sidebar-foreground/50">Small progress is still a signal.</p></div>
      <div className="absolute bottom-5 left-4 right-4"><Link href="/admin" className="flex items-center gap-3 px-3 py-2.5 text-xs text-sidebar-foreground/45 transition-colors hover:text-sidebar-foreground" data-testid="link-admin"><Settings size={15} /> Admin view</Link><div className="mt-2 flex items-center gap-3 border-t border-sidebar-border px-3 pt-4"><span className="grid size-8 place-items-center rounded-full bg-secondary text-xs font-semibold text-primary">{student.initials}</span><div className="min-w-0"><p className="truncate text-xs font-semibold">{student.name}</p><p className="truncate text-[10px] text-sidebar-foreground/45">{student.role}</p></div></div></div>
    </aside>
    {mobileNav && <button className="fixed inset-0 z-20 bg-primary/30 lg:hidden" onClick={() => setMobileNav(false)} aria-label="Close navigation overlay" data-testid="button-overlay-nav" />}
    <div className="lg:pl-[244px]"><header className="sticky top-0 z-10 flex h-[73px] items-center justify-between border-b border-border bg-background/95 px-5 backdrop-blur-sm lg:px-9"><button className="grid size-9 place-items-center border border-border lg:hidden" onClick={() => setMobileNav(true)} aria-label="Open navigation" data-testid="button-open-nav"><Menu size={18} /></button><div className="hidden font-mono-ui text-[10px] uppercase tracking-[.18em] text-muted-foreground lg:block">30-day AI mastermind / {student.currentDay < 10 ? `0${student.currentDay}` : student.currentDay}</div><div className="ml-auto flex items-center gap-4"><button className="text-muted-foreground transition-colors hover:text-foreground" aria-label="Notifications" data-testid="button-notifications"><Bell size={17} /></button><div className="hidden h-5 w-px bg-border sm:block" /><span className="hidden text-xs text-muted-foreground sm:block" data-testid="text-header-student">{student.name}</span><span className="grid size-8 place-items-center rounded-full bg-accent text-xs font-semibold">{student.initials}</span></div></header><main className="mx-auto max-w-[1320px] p-5 lg:p-9">{children}</main></div>
  </div>;
}

function ProgressBar({ value }: { value: number }) { return <div className="h-1.5 w-full overflow-hidden bg-muted"><div className="h-full bg-accent transition-all duration-500" style={{ width: `${value}%` }} /></div>; }

function Dashboard() {
  const { completedSessions } = useLearnerStore();
  const progress = Math.round((completedSessions.length / sessions.length) * 100);
  const nextSession = sessions.find((session) => !completedSessions.includes(session.id)) ?? sessions[sessions.length - 1];
  return <AppShell><PageMeta title="Your studio" /><div className="reveal"><div className="flex flex-col justify-between gap-6 sm:flex-row sm:items-end"><div><p className="font-mono-ui text-[10px] uppercase tracking-[.2em] text-accent">Tuesday, 24 September</p><h1 className="mt-3 text-4xl tracking-[-.045em] sm:text-5xl">Keep the signal moving, <span className="font-display italic">Aarav.</span></h1><p className="mt-3 text-sm text-muted-foreground">You are on day {student.currentDay} of your 30-day build.</p></div><div className="flex items-center gap-3 border border-border bg-card px-4 py-3"><div className="grid size-9 place-items-center rounded-full bg-accent"><Sparkles size={17} /></div><div><p className="text-xs font-semibold">4 day streak</p><p className="mt-0.5 text-[11px] text-muted-foreground">Momentum compounds</p></div></div></div>
      <div className="mt-10 grid gap-4 lg:grid-cols-[1.35fr_.65fr]"><section className="relative overflow-hidden bg-primary p-6 text-primary-foreground sm:p-8"><div className="absolute -right-20 -top-20 size-60 rounded-full border border-secondary/20" /><div className="relative"><div className="flex items-center justify-between"><span className="font-mono-ui text-[10px] uppercase tracking-[.17em] text-secondary">Next up / Day {nextSession.day}</span><span className="font-mono-ui text-[10px] text-primary-foreground/45">{nextSession.duration}</span></div><h2 className="mt-12 max-w-md text-4xl leading-[.94] tracking-[-.04em] sm:text-5xl">{nextSession.title}</h2><p className="mt-4 max-w-md text-sm leading-6 text-primary-foreground/60">{nextSession.description}</p><Link href={`/app/sessions/${nextSession.id}`} className="mt-8 inline-flex items-center gap-2 bg-secondary px-4 py-3 text-sm font-semibold text-primary transition-transform hover:-translate-y-1" data-testid="link-next-session">Continue session <ArrowRight size={15} /></Link></div></section><section className="border border-border bg-card p-6 sm:p-8"><div className="flex items-center justify-between"><span className="font-mono-ui text-[10px] uppercase tracking-[.17em] text-muted-foreground">Your progress</span><span className="text-2xl font-semibold">{progress}%</span></div><ProgressBar value={progress} /><p className="mt-4 text-sm leading-6 text-muted-foreground">One session complete. The next useful move is already queued.</p><div className="mt-8 grid grid-cols-2 gap-3 border-t border-border pt-5"><div><p className="text-2xl font-semibold">{completedSessions.length}</p><p className="mt-1 text-xs text-muted-foreground">sessions complete</p></div><div><p className="text-2xl font-semibold">1</p><p className="mt-1 text-xs text-muted-foreground">answer saved</p></div></div></section></div>
      <div id="sessions" className="mt-16"><div className="flex items-end justify-between"><div><p className="font-mono-ui text-[10px] uppercase tracking-[.2em] text-accent">Your path</p><h2 className="mt-2 text-2xl tracking-[-.03em]">Sessions in sequence</h2></div><span className="hidden text-xs text-muted-foreground sm:block">One idea at a time</span></div><div className="mt-5 grid gap-3">{sessions.map((session) => { const done = completedSessions.includes(session.id); return <Link href={`/app/sessions/${session.id}`} key={session.id} className="group grid gap-4 border border-border bg-card p-4 transition-all hover:-translate-y-0.5 hover:border-foreground/30 sm:grid-cols-[48px_1fr_auto] sm:items-center" data-testid={`card-session-${session.id}`}><span className={`grid size-10 place-items-center rounded-full text-xs font-semibold ${done ? 'bg-secondary text-primary' : 'border border-border text-muted-foreground'}`}>{done ? <Check size={16} /> : `0${session.day}`}</span><div><div className="flex flex-wrap items-center gap-2"><p className="font-semibold">{session.title}</p>{done && <span className="font-mono-ui text-[9px] uppercase tracking-widest text-accent">Complete</span>}</div><p className="mt-1 text-sm text-muted-foreground">{session.label}</p></div><div className="flex items-center justify-between gap-5 text-xs text-muted-foreground sm:justify-end"><span>{session.duration}</span><ArrowRight size={16} className="transition-transform group-hover:translate-x-1" /></div></Link>; })}</div></div>
      <div id="assignments" className="mt-16 grid gap-8 lg:grid-cols-[1.2fr_.8fr]"><div><div className="flex items-end justify-between"><div><p className="font-mono-ui text-[10px] uppercase tracking-[.2em] text-accent">Practice room</p><h2 className="mt-2 text-2xl tracking-[-.03em]">Assignments</h2></div><span className="text-xs text-muted-foreground">{assignments.length} prompts</span></div><div className="mt-5 grid gap-3">{assignments.map((assignment) => <Link href={`/app/assignments/${assignment.id}`} key={assignment.id} className="group flex items-center justify-between border border-border bg-card p-4 hover:border-foreground/30" data-testid={`card-assignment-${assignment.id}`}><div className="flex items-center gap-3"><span className="grid size-9 place-items-center bg-muted font-mono-ui text-[10px]">0{assignment.day}</span><div><p className="text-sm font-semibold">{assignment.title}</p><p className="mt-1 text-xs text-muted-foreground">Day {assignment.day} practice</p></div></div><ArrowRight size={16} className="text-muted-foreground transition-transform group-hover:translate-x-1" /></Link>)}</div></div><aside className="border border-border bg-accent/15 p-6"><div className="flex items-center gap-2 text-accent"><Headphones size={17} /><span className="font-mono-ui text-[10px] uppercase tracking-[.17em]">Studio note</span></div><p className="mt-8 font-display text-3xl leading-none">“Build the smallest thing that teaches you the most.”</p><p className="mt-6 text-xs leading-5 text-muted-foreground">A reminder from your mentor for the moments when the idea starts getting too big.</p></aside></div>
    </div></AppShell>;
}

function SessionPlayer() {
  const { sessionId } = useParams<{ sessionId: string }>();
  const [, setLocation] = useLocation();
  const { completedSessions, markSessionComplete } = useLearnerStore();
  const session = sessions.find((item) => item.id === sessionId);
  if (!session) return <MissingContent label="Session not found" />;
  const done = completedSessions.includes(session.id);
  const complete = () => { markSessionComplete(session.id); const next = sessions.find((item) => item.day > session.day); setLocation(next ? `/app/sessions/${next.id}` : '/app'); };
  return <AppShell><PageMeta title={`${session.title} — Session`} /><div className="reveal"><Link href="/app" className="inline-flex items-center gap-2 text-xs text-muted-foreground hover:text-foreground" data-testid="link-back-dashboard"><ArrowLeft size={14} /> Back to your studio</Link><div className="mt-8 flex flex-wrap items-end justify-between gap-4"><div><p className="font-mono-ui text-[10px] uppercase tracking-[.2em] text-accent">Day {session.day} / {session.label}</p><h1 className="mt-3 text-4xl tracking-[-.045em] sm:text-6xl">{session.title}</h1></div><span className="flex items-center gap-2 text-xs text-muted-foreground"><Clock3 size={14} /> {session.duration}</span></div><div className="mt-8 grid gap-8 lg:grid-cols-[1.35fr_.65fr]"><div><div className="relative aspect-video overflow-hidden bg-primary"><div className="absolute inset-0 opacity-20" style={{ backgroundImage: 'linear-gradient(rgba(216,240,75,.35) 1px, transparent 1px), linear-gradient(90deg, rgba(216,240,75,.35) 1px, transparent 1px)', backgroundSize: '32px 32px' }} /><div className="absolute left-8 top-8 font-mono-ui text-[10px] uppercase tracking-[.2em] text-secondary">Studio lecture / {String(session.day).padStart(2, '0')}</div><div className="absolute inset-0 grid place-items-center"><button className="grid size-16 place-items-center rounded-full bg-secondary text-primary transition-transform hover:scale-105" aria-label="Play lecture" data-testid="button-play-lecture"><Play size={22} fill="currentColor" className="ml-1" /></button></div><div className="absolute bottom-0 left-0 right-0 flex items-center gap-3 p-5"><span className="h-1 flex-1 bg-primary-foreground/25"><span className="block h-full w-[34%] bg-accent" /></span><span className="font-mono-ui text-[10px] text-primary-foreground/60">06:12 / {session.duration}</span></div></div><div className="mt-8"><p className="font-mono-ui text-[10px] uppercase tracking-[.2em] text-muted-foreground">The useful bit</p><p className="mt-3 max-w-2xl text-xl leading-8 tracking-[-.02em]">{session.takeaway}</p></div></div><aside className="border border-border bg-card p-5 sm:p-7"><p className="font-mono-ui text-[10px] uppercase tracking-[.18em] text-accent">Session kit</p><div className="mt-6 divide-y divide-border">{session.resources.map((resource) => <button key={resource} className="flex w-full items-center justify-between py-4 text-left text-sm hover:text-accent" onClick={() => window.alert(`${resource} will open when the resource library is connected.`)} data-testid={`button-resource-${resource.toLowerCase().replaceAll(' ', '-')}`}><span className="flex items-center gap-3"><FileText size={15} className="text-muted-foreground" />{resource}</span><ArrowUpRightIcon /></button>)}</div><div className="mt-7 border-t border-border pt-5"><Button onClick={complete} variant={done ? 'outline' : 'accent'} className="w-full" data-testid="button-complete-session">{done ? <><Check size={15} /> Completed</> : <>Mark session complete <ArrowRight size={15} /></>}</Button></div></aside></div><div className="mt-10 max-w-3xl border-t border-border pt-8"><div className="flex items-center justify-between"><div><p className="font-mono-ui text-[10px] uppercase tracking-[.2em] text-muted-foreground">Transcript</p><h2 className="mt-2 text-2xl tracking-tight">Read along</h2></div><button className="text-xs font-semibold underline underline-offset-4" onClick={() => window.alert('Transcript copied to your clipboard.')} data-testid="button-copy-transcript">Copy transcript</button></div><div className="mt-6 space-y-5 text-sm leading-7 text-muted-foreground">{session.transcript.map((line, index) => <p key={line}><span className="mr-4 font-mono-ui text-[10px] text-accent">0{index + 1}</span>{line}</p>)}</div></div></div></AppShell>;
}

function AssignmentPage() {
  const { assignmentId } = useParams<{ assignmentId: string }>();
  const assignment = assignments.find((item) => item.id === assignmentId);
  const { answers, feedback, saveAnswer, requestFeedback } = useLearnerStore();
  const [answer, setAnswer] = useState(assignment ? answers[assignment.id] ?? '' : '');
  const [saved, setSaved] = useState(Boolean(assignment && answers[assignment.id]));
  if (!assignment) return <MissingContent label="Assignment not found" />;
  const save = () => { saveAnswer(assignment.id, answer); setSaved(true); };
  return <AppShell><PageMeta title={`${assignment.title} — Assignment`} /><div className="reveal"><Link href="/app#assignments" className="inline-flex items-center gap-2 text-xs text-muted-foreground hover:text-foreground" data-testid="link-back-assignments"><ArrowLeft size={14} /> Back to assignments</Link><div className="mt-8 max-w-3xl"><p className="font-mono-ui text-[10px] uppercase tracking-[.2em] text-accent">Day {assignment.day} / Practice room</p><h1 className="mt-3 text-4xl leading-none tracking-[-.045em] sm:text-6xl">{assignment.title}</h1><p className="mt-6 text-lg leading-8 text-muted-foreground">{assignment.prompt}</p></div><div className="mt-10 grid gap-8 lg:grid-cols-[1.25fr_.75fr]"><div><label className="block"><span className="font-mono-ui text-[10px] uppercase tracking-[.18em] text-muted-foreground">Your working answer</span><textarea value={answer} onChange={(e) => { setAnswer(e.target.value); setSaved(false); }} placeholder="Start with the honest version..." rows={11} className="mt-3 w-full resize-y border border-border bg-card p-5 text-sm leading-7 outline-none transition-colors placeholder:text-muted-foreground/60 focus:border-primary" data-testid="textarea-assignment-answer" /></label><div className="mt-4 flex flex-wrap items-center gap-3"><Button onClick={save} data-testid="button-save-answer"><Check size={15} /> {saved ? 'Answer saved' : 'Save answer'}</Button><Button onClick={() => { save(); requestFeedback(assignment.id); }} variant="outline" data-testid="button-request-feedback"><Sparkles size={15} /> Ask for AI feedback</Button><span className="text-xs text-muted-foreground">{saved ? 'Saved just now' : 'Unsaved changes'}</span></div></div><aside className="h-fit border border-border bg-accent/15 p-6"><div className="flex items-center gap-2 text-accent"><Sparkles size={16} /><span className="font-mono-ui text-[10px] uppercase tracking-[.18em]">A useful constraint</span></div><p className="mt-7 text-lg leading-7">{assignment.hint}</p><div className="mt-7 border-t border-border/70 pt-5"><p className="font-mono-ui text-[10px] uppercase tracking-[.16em] text-muted-foreground">Deliverable</p><p className="mt-2 text-sm leading-6 text-muted-foreground">{assignment.deliverable}</p></div></aside></div>{feedback[assignment.id] && <section className="mt-10 max-w-3xl border border-secondary/60 bg-secondary/20 p-6 sm:p-8"><div className="flex items-center gap-2 text-sm font-semibold"><Sparkles size={16} /> AI feedback</div><p className="mt-4 text-sm leading-7 text-foreground/75" data-testid="status-ai-feedback">{feedback[assignment.id]}</p></section>}</div></AppShell>;
}

function Admin() {
  const [filter, setFilter] = useState('All students');
  const [notice, setNotice] = useState('');
  const [students, setStudents] = useState([{ name: 'Aarav Mehta', initials: 'AM', progress: 28, status: 'Active', last: '12 min ago' }, { name: 'Priya Nair', initials: 'PN', progress: 64, status: 'Active', last: '42 min ago' }, { name: 'Rohan Kapoor', initials: 'RK', progress: 12, status: 'Needs nudge', last: '2 days ago' }, { name: 'Meera Shah', initials: 'MS', progress: 91, status: 'Active', last: 'Yesterday' }]);
  const filtered = filter === 'All students' ? students : students.filter((item) => filter === item.status);
  return <AppShell><PageMeta title="Admin dashboard" /><div className="reveal"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="font-mono-ui text-[10px] uppercase tracking-[.2em] text-accent">Admin / Programme room</p><h1 className="mt-3 text-4xl tracking-[-.045em] sm:text-5xl">See the room clearly.</h1><p className="mt-3 text-sm text-muted-foreground">Manage learners, sessions and the conversations that keep momentum alive.</p></div><Button onClick={() => setNotice('Invite link copied to clipboard.')} variant="accent" data-testid="button-invite-student"><Plus size={16} /> Invite student</Button></div>{notice && <div className="mt-5 flex items-center justify-between border border-secondary/60 bg-secondary/20 px-4 py-3 text-sm"><span>{notice}</span><button onClick={() => setNotice('')} aria-label="Dismiss notice" data-testid="button-dismiss-notice"><X size={16} /></button></div>}<div className="mt-10 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{[{ label: 'Active learners', value: '437,787+', detail: 'all-time reach', icon: <Users size={17} /> }, { label: 'Programme progress', value: '46.8%', detail: 'average this cohort', icon: <BarChart3 size={17} /> }, { label: 'Open submissions', value: '24', detail: 'awaiting review', icon: <FileText size={17} /> }, { label: 'Live sessions', value: '04', detail: 'this month', icon: <Play size={17} /> }].map((stat) => <div key={stat.label} className="border border-border bg-card p-5"><div className="flex items-center justify-between text-accent"><span className="font-mono-ui text-[9px] uppercase tracking-[.15em] text-muted-foreground">{stat.label}</span>{stat.icon}</div><p className="mt-7 text-3xl font-semibold tracking-[-.04em]">{stat.value}</p><p className="mt-1 text-xs text-muted-foreground">{stat.detail}</p></div>)}</div><div className="mt-12 grid gap-8 xl:grid-cols-[1.35fr_.65fr]"><section className="border border-border bg-card"><div className="flex flex-col gap-4 border-b border-border p-5 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-lg font-semibold">Student management</h2><p className="mt-1 text-xs text-muted-foreground">A calm view of who needs your attention.</p></div><div className="flex items-center gap-2"><Search size={15} className="text-muted-foreground" /><select value={filter} onChange={(e) => setFilter(e.target.value)} className="bg-transparent text-xs outline-none" data-testid="select-student-filter"><option>All students</option><option>Active</option><option>Needs nudge</option></select></div></div><div className="overflow-x-auto"><table className="w-full min-w-[560px] text-left text-sm"><thead className="border-b border-border bg-muted/50 font-mono-ui text-[9px] uppercase tracking-[.15em] text-muted-foreground"><tr><th className="px-5 py-3 font-normal">Student</th><th className="px-5 py-3 font-normal">Progress</th><th className="px-5 py-3 font-normal">Status</th><th className="px-5 py-3 font-normal">Last seen</th><th className="px-5 py-3 font-normal" /></tr></thead><tbody className="divide-y divide-border">{filtered.map((item) => <tr key={item.name} className="group"><td className="px-5 py-4"><div className="flex items-center gap-3"><span className="grid size-8 place-items-center rounded-full bg-muted text-[10px] font-semibold">{item.initials}</span><span className="font-semibold">{item.name}</span></div></td><td className="px-5 py-4"><div className="flex items-center gap-3"><div className="w-20"><ProgressBar value={item.progress} /></div><span className="text-xs text-muted-foreground">{item.progress}%</span></div></td><td className="px-5 py-4"><span className={`font-mono-ui text-[9px] uppercase tracking-[.12em] ${item.status === 'Needs nudge' ? 'text-accent' : 'text-muted-foreground'}`}>{item.status}</span></td><td className="px-5 py-4 text-xs text-muted-foreground">{item.last}</td><td className="px-5 py-4 text-right"><button className="text-muted-foreground hover:text-foreground" onClick={() => setNotice(`Opening ${item.name}'s learner profile.`)} aria-label={`Open ${item.name}`} data-testid={`button-open-student-${item.initials}`}><MoreHorizontal size={17} /></button></td></tr>)}</tbody></table></div></section><section className="border border-border bg-primary p-6 text-primary-foreground"><div className="flex items-center justify-between"><p className="font-mono-ui text-[10px] uppercase tracking-[.17em] text-secondary">Submission pulse</p><ChevronDown size={15} className="text-primary-foreground/45" /></div><div className="mt-12"><p className="font-display text-4xl leading-none">24 answers<br /><em className="text-secondary">in the room.</em></p><p className="mt-5 text-sm leading-6 text-primary-foreground/55">The highest response is for the opportunity thesis. People are finding the edge.</p></div><button onClick={() => setNotice('Submission queue opened.')} className="mt-10 flex items-center gap-2 border-b border-secondary pb-2 text-sm font-semibold text-secondary" data-testid="button-open-submissions">Review submissions <ArrowRight size={15} /></button></section></div><div className="mt-8 grid gap-8 lg:grid-cols-2"><section className="border border-border bg-card p-5"><div className="flex items-center justify-between"><div><h2 className="text-lg font-semibold">Session management</h2><p className="mt-1 text-xs text-muted-foreground">The current programme sequence.</p></div><button className="text-xs font-semibold text-accent" onClick={() => setNotice('Session editor opened.')} data-testid="button-edit-sessions">Edit sequence</button></div><div className="mt-5 grid gap-2">{sessions.map((item) => <div key={item.id} className="flex items-center gap-3 border-t border-border py-3"><span className="font-mono-ui text-[10px] text-accent">0{item.day}</span><span className="flex-1 text-sm">{item.title}</span><span className="text-xs text-muted-foreground">{item.duration}</span><CheckCircle2 size={15} className="text-secondary" /></div>)}</div></section><section className="border border-border bg-card p-5"><div className="flex items-center justify-between"><div><h2 className="text-lg font-semibold">Recent notes</h2><p className="mt-1 text-xs text-muted-foreground">Messages worth carrying into the room.</p></div><button className="text-xs font-semibold text-accent" onClick={() => setNotice('Note composer opened.')} data-testid="button-new-note">New note</button></div><div className="mt-5 grid gap-3"><div className="border-l-2 border-accent pl-4"><p className="text-sm leading-6">“Keep the first offer smaller than feels comfortable.”</p><p className="mt-2 font-mono-ui text-[9px] uppercase tracking-widest text-muted-foreground">Lakshit / Today</p></div><div className="border-l-2 border-secondary pl-4"><p className="text-sm leading-6">Cohort 04 is responding strongly to positioning prompts.</p><p className="mt-2 font-mono-ui text-[9px] uppercase tracking-widest text-muted-foreground">Studio system / Yesterday</p></div></div></section></div></div></AppShell>;
}

function MissingContent({ label }: { label: string }) { return <AppShell><div className="grid min-h-[60vh] place-items-center text-center"><div><div className="mx-auto grid size-14 place-items-center rounded-full bg-muted"><Search size={20} /></div><h1 className="mt-5 text-2xl">{label}</h1><p className="mt-2 text-sm text-muted-foreground">This path moved, but your next step is still here.</p><Link href="/app" className="mt-6 inline-flex items-center gap-2 bg-primary px-4 py-3 text-sm font-semibold text-primary-foreground" data-testid="link-missing-dashboard">Return to dashboard <ArrowRight size={15} /></Link></div></div></AppShell>; }

function Router() {
  const [location] = useLocation();
  return <ErrorBoundary resetKey={location}><Switch><Route path="/" component={Home} /><Route path="/login"><AuthPage mode="login" /></Route><Route path="/signup"><AuthPage mode="signup" /></Route><Route path="/app" component={Dashboard} /><Route path="/app/sessions/:sessionId" component={SessionPlayer} /><Route path="/app/assignments/:assignmentId" component={AssignmentPage} /><Route path="/admin" component={Admin} /><Route component={NotFound} /></Switch></ErrorBoundary>;
}

function App() {
  return <QueryClientProvider client={queryClient}><TooltipProvider><LearnerProvider><WouterRouter base={import.meta.env.BASE_URL.replace(/\/$/, '')}><Router /></WouterRouter></LearnerProvider><Toaster /></TooltipProvider></QueryClientProvider>;
}

export default App;